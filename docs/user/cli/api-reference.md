# fsspec-cli API Reference

The public surface is small and deliberately so: one class, its policy types,
and the context object extensions read.

::: fsspec_cli

## Extensions

The bundled backend-specific command, provided as a worked example of the
extension seam:

::: fsspec_cli.extensions
