# vosfs

`vosfs` is the Python package for VOSpace filesystem support.

The project currently exposes a small typed public interface while filesystem
features are developed. See the [API Reference](api-reference.md) for the API
available in the current source.

To contribute, follow the
[contributor guide](https://github.com/shinybrar/vosfs/blob/main/CONTRIBUTING.md).
