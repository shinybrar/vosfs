# fsspec-cli

`fsspec-cli` is a library-only package for composing POSIX-shaped commands
over host-configured async fsspec filesystems.

Version 0.1.0 establishes the independently installable package boundary. It
does not yet expose commands, an application API, a console entry point, or a
module executable.
